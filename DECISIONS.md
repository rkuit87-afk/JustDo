Decision #0006: Component Tracking module added (2026-06-15). Three new tables: component_categories, equipment_components, component_events. Nine new API routes under /api/component-categories and /api/components/<id>. New "🔩 Components" nav tab on admin dashboard (visible to admin role only). Features: per-machine component register, initial condition assessment (Unknown/New/Good/Fair/Poor/Critical), component heartbeat (installation date, running age, replacement count, failure count), replacement event logging (resets heartbeat), failure logging, event history. Passive learning hooks (auto-update from PM/breakdown completion) deferred to Phase 2 — pending Roelof review. Prompt specification stored in Prompts/COMPONENT_TRACKING_PLUGIN_PROMPT.md.

Decision #0001: The Truth Comes First replaces The Artisan Comes First as the primary guiding principle because it is more universal, more objective, and still preserves the project's commitment to reducing unnecessary burden on the people doing the work. The system exists to reveal reality, not to protect any role or hierarchy.

Decision #0002: Introduced stores_admin, team_leader, picker, and storeman roles. Coetser Steven is stores_admin — limited to Stores and Team tabs, can only add picker/storeman staff. Kock Divan, Strydom Francois, Hlophe Given are team_leaders — appear on the artisan login tab with transparent green background, land on the artisan board with a Requisitions tab, and can submit PRs which stores_admin can action. Managers and team_leaders see only their own PR status. Admin can query status from stores.

Decision #0004: Procurement tracking and storeman receive workflow. Requisitions gain: quote_ref (auto-generated QT-YYYY-NNNNNN when ordered), supplier_name (entered by Steven), invoice_ref (entered by storeman at delivery). Status states revised: submitted → pending_approval → pending_quotation → ordered → delivered OR partially_received. Declined added as a terminal state (reason saved in action_notes). Storeman receive tab redesigned with two modes: "Receive Against Order" (search by QT ref or PR number, enter invoice, confirm full or partial) and "Ad-hoc Direct" (existing item dropdown). Stock qty_on_hand auto-updates for PR line items that have a stock_id linked. stock_movements gains a requisition_id column for audit trail. Manager and team leader PR view shows quote_ref, supplier, invoice ref and decline reason where applicable.

Decision #0005: Activity log color coding and Req. Handling exclamation badges added. Action badges are now color-coded: green = completed/resolved/received/added/updated settings, amber = logged/submitted/requested/suspended/actioned, red = queried/deactivated, blue = allocated/added/updated (equipment/users/modes). Req. Handling nav tab shows a red pill counter (❗N) when any PRs have admin_queried=1; each queried PR card shows bold red ❗ Queried by Admin inline with the PR number. Stores tab restored to admin dashboard — the bleed fix that hid stores-nav-tab for admin users was partially reverted; only req-handling-nav-tab remains hidden for admin (that tab belongs to stores_admin). Store staff (picker/storeman) navigation fix: showScreen() prefix map now includes picker→store-picker so Receive and Issue tabs navigate correctly.

Decision #0003: Stores admin overhaul and role colour standardisation. Steven's board now lands on a dedicated "Req. Handling" tab showing all PRs sorted unhandled-first, with orange highlight for PRs older than 3 days and red for older than 5 days. Admin-queried PRs are flagged purple. Requisition status states expanded from binary submitted/ordered to four post-action states: Pending Approval, Pending Quotation, Ordered, Arrived — stores_admin selects from a modal rather than a free-text prompt. Admin can query (flag) a PR which sets admin_queried=1 in the database via migration. Managers and team_leaders see status only, never a query button. Pickers/storemen have no requisition access (they land on pickerBoard). Role colour convention: Admin=red, Store=purple, Team Leader=brown; Artisan and Manager unchanged. Store Staff login group added as a fifth role-card on the login screen. Millwright removed from artisan sub-role filter (not a real trade in this mill). Admin board nav tab restoration bug fixed — showAdminBoard() now resets all tabs before re-hiding stores-specific ones, preventing bleed from a prior stores_admin session.
